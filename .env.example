# Admin credentials
ADMIN_EMAIL=joelfrenette@gmail.com
# Generate via `node -e "const bcrypt=require('bcryptjs'); bcrypt.hash('Sandals2026!', 12).then(console.log)"`
ADMIN_PASSWORD_HASH=

# Random 32+ char secret used for signing session cookies
SESSION_SECRET=
